# AETHERQOR V6 URGENT — CLAUDE CODE HANDOFF

## Mission

Resolve four remaining production architecture areas and the gameplay identity contract using evidence + real AETHERQOR experiments. Alpha Wrap is CLOSED and must not be researched again.

Local V6 root after workflow completion:
`D:\AetherqorFoundry\research\blockers_v6_urgent\ULTRA_RESEARCH_2026-09-05_URGENT`

Reuse prior local evidence:
- V3: `D:\AetherqorFoundry\research\blockers_v3\CLAUDE_VIDEO_REPORT_33904162671`
- V4: `D:\AetherqorFoundry\research\blockers_v4_night\ULTRA_RESEARCH_2026-09-04_NIGHT`
- V5: `D:\AetherqorFoundry\research\blockers_v5_architecture\ULTRA_RESEARCH_2026-09-05`

Do not redownload prior videos when an existing local complete set contains the same source.

## Mandatory reading order

1. `LOCAL_READY.md`
2. `QUESTIONS.md`
3. `SOURCE_REGISTRY.md`
4. `CHATGPT_INDEPENDENT_ANALYSIS.md`
5. `PRELIMINARY_VERDICTS.md`
6. `FINAL_STATUS.md`
7. V6 video evidence: manifest -> transcript -> frame/sheet/high-detail indexes -> 3x3 sheets -> selected 1fps -> selected 4fps
8. relevant V3/V4/V5 local evidence
9. current AETHERQOR code, asset bible and measured QA artifacts

Every external statement must be tagged by evidence class. A Sketchfab/reupload triangle count is never an official studio limit.

## Hard execution contract

For each problem:
`READ PRIMARY SOURCES -> REVIEW VIDEO FRAMES -> AUDIT CURRENT CODE/ASSET -> FORM HYPOTHESIS -> BUILD MINIMAL REAL-ASSET VARIANT -> MEASURE -> PROOF RENDER/PROFILE -> DECIDE -> IMPLEMENT -> AUTOMATED QA -> CHECKPOINT COMMIT`

You are not finished because you read sources. You are finished when AETHERQOR has a measured production decision.

# P1 + P2 FIRST — BUDGET ARCHITECTURE AND THICKNESS

Treat these together because full Solidify may be a major cause of the 14k impossibility.

## Step P1.1: assembled visibility audit

Find the current complete representative outfit and write `artifacts/research/v6/assembled_visibility.csv` containing for every runtime object:
- logical slot
- object/renderer/mesh
- triangles before modifiers
- triangles after modifiers
- outer-facing triangles
- triangles generated by Solidify / hidden inner surfaces
- material count
- skinned/static
- body-hide relationship
- hidden by another equipped layer yes/no
- visible at gameplay 35° azimuth / 50° elevation yes/no
- visible equipment close-up yes/no

Report:
- catalog sum
- assembled renderer sum
- visible gameplay sum
- visible close-up sum

Do NOT equate 19 logical slots with 19 fully visible high-detail surfaces.

## Step P2.1: remove global full-thickness assumption

For cuirass, helmet and shoulder build three non-destructive variants:
A current full Solidify
B one-sided outer shell with backface culling / URP Render Face Front
C one-sided outer shell + local edge sidewalls/rims + only necessary inner patches

For C:
- locate exposed boundary loops/openings;
- create physical sidewall thickness only around camera-visible edges;
- use 1–2 inner edge rings if required for grazing-angle continuity;
- do not create a full hidden inner face unless a sampled camera/animation can see it;
- keep rigid material culling ON by default.

Measure per variant:
- triangles
- post-split vertices
- renderer/batch count
- silhouette/edge readability
- visible holes/paper-edge events from 24 turntable angles + gameplay camera + equipment camera
- animation clipping
- GPU timing on best target-like mobile profile/device available

If C saves meaningful geometry without visible regression, migrate rigid plates to this architecture before selecting a new total budget.

## Step P1.2: total-character quality ladder

After the best thickness architecture is chosen, create the same complete outfit at total visible budgets:
- 14k
- 20k
- 28k
- 40k

Do not simply decimate one mesh four times. Allocate geometry intentionally using the P3 allocator/protected constraints where possible.

Render identical key poses/animation frames at character heights:
90, 150, 220, 300 px
plus equipment close-up.

Metrics:
- silhouette IoU vs 40k reference
- normalized robust contour Hausdorff
- rigid edge-map difference / faceting evidence
- animation clipping
- rendered triangles and vertices
- SkinnedMeshRenderer count
- draw calls
- GPU frame time

Write `artifacts/research/v6/P1_BUDGET_DECISION.md` with one of:
- KEEP_14K_GAMEPLAY_LOW_ONLY
- KEEP_14K_UNIVERSAL (only if proven)
- ADOPT_CONTEXT_LODS with explicit budgets
- ADOPT_NEW_GAMEPLAY_BUDGET with measured value

No decision may cite a competitor internet mesh count as the sole reason.

# P3 — FREE CONTROLLED REDUCTION / 19 MODULAR SEAMS

Inspect `retopo_postaci.py` and locate every second-pass reduction. Produce a before/after table for approved slot geometry.

## P3.1 Blender native test

Prototype Decimate Collapse using vertex groups on a copy. Confirm actual weight semantics with a tiny deterministic test before using them on production geometry. Mark:
- seam vertices
- silhouette-critical vertices
- deformation-critical vertices
- hard plate edge loops

Test symmetry.

## P3.2 hard seam lock

Test whether the installed Python CGAL package exposes Surface_mesh_simplification constrained-edge APIs. If it does:
- mark canonical modular border edges constrained;
- use constrained placement so border points do not move;
- simplify interior only.

If those Python bindings are not exposed, do not block the project. Use Blender's protected vertex groups / selective interior reduction first and record CGAL SMS as optional engineering.

## P3.3 canonical seam protocol

Implement a reusable seam data format:
- seam_id
- ordered vertices
- canonical direction
- coordinates
- tolerance
- optional normals/UV boundary metadata

Mating slots must share the same canonical seam result. After reduction assert exact/within-tolerance equality of seam vertex count and ordered positions.

The current shoulder 16/16 regression becomes an automated test. A 11/11 vs 11/12 result is an unconditional FAIL.

## P3.4 global allocator

Remove the single whole-character ratio as the production architecture.

For every slot generate legal quality candidates containing:
- triangle count
- silhouette error at target screen sizes
- deformation error
- seam PASS/FAIL
- visual score
- min/target/max classification

Implement a deterministic allocator that starts at required quality and picks the next downgrade with the lowest error increase per triangle saved, never crossing hard seam/silhouette/deformation constraints.

If no legal combination fits the target, return `BUDGET_ARCHITECTURE_IMPOSSIBLE`. Never silently damage an approved slot to make the number green.

# P4 — HAIR REGULAR OCCLUSION BOUNDARIES

Do not repeat:
- scalp normal transfer
- cap-only SEG increase
- the previous protruding solid dome
- duplicated inverted normals

Select one known failing hairstyle and stay approximately in the 1800-triangle envelope.

Build controlled geometry variants:
A current flat regular overlap
B stagger adjacent root rows and card-end phase
C B + more curvature only on primary cards whose occlusion boundaries are visible
D trimmed hidden opaque/core hair mass under the hairline + irregular card layers

Starting variation sweep, not final constants:
- row phase offset: 0.3 / 0.4 / 0.5 of local card spacing
- length variation: ±15 / ±20 / ±25%
- controlled root/radial offset: small millimeter-scale values relative to the hairstyle
- bend subdivision allocated only to visually important primary/top cards

For D, the opaque inner layer must never protrude as the previous rectangular dome did. Trim its boundary under the visible hairline and first card rows.

Then compare materials on the best geometry:
- Alpha Clip
- current Dithered
- Alpha Blend

Use URP mobile settings and Render Face Both only for actual two-sided hair cards. Render CAMERA MOTION, not only stills, at 90/100/150/220 px with 1, 5 and 10 characters.

Measure:
- scalp exposure
- silhouette
- aligned occlusion/facet events
- sorting errors
- temporal flicker/popping
- overdraw
- GPU time

Create `P4_HAIR_MOBILE_DECISION.md` with separate settings for GAMEPLAY and EQUIPMENT_CLOSEUP if needed.

# P5 — TOP-DOWN CLASS IDENTITY

Existing IoU gate stays as a regression signal but is not sufficient.

Generate all seven classes from the real camera:
- azimuth 35°
- elevation 50°
- heights 64, 90, 128, 150, 220 px

Create ablations:
A black silhouette
B silhouette + weapon/prop/primary anchor
C normal value/color/material
D C + representative idle pose
E optional stable class VFX if it truly exists in all relevant gameplay states

Add metrics:
- IoU
- Dice
- 95th percentile Hausdorff normalized by height
- contour Chamfer
- 8/16-sector radial shape descriptor
- negative-space measure
- OUTER_CONTOUR_CONTRIBUTION of the designated class anchor

The current >=10% anchor-area metric must not be treated as equivalent to contour contribution.

Build a tiny local timed recognition harness: show randomized class render 500–1000 ms, then force one of 7 labels. Record accuracy, response time and confusion matrix. Use multiple repetitions and, if practical, several naive testers.

Calibrate automatic thresholds against human confusion. Do not invent an 'industry IoU threshold'.

If mage/seer remain confused, test primary cues that survive top view without forcing a long weapon:
- asymmetric shoulders/hood
- compact back focus/halo
- cape split / negative space
- asymmetric sleeve/arm pose
- distinctive headpiece
- floating focus/orb
- strong upper-body value/color block

Required decision: each class needs a stable PRIMARY gameplay cue, but it need not be a long weapon.

# Documentation / commits

For each P1–P5 create `artifacts/research/v6/Px_DECISION.md` with:
- measured current state
- source evidence
- video evidence with slug + timecode + sheet/frame
- hypothesis
- experiment
- raw results
- proof renders
- performance data
- production decision
- implementation
- automated QA
- commit

Checkpoint commits are mandatory, e.g.:
- `research(v6): audit assembled character budget architecture`
- `feat(gear): replace full rigid solidify with selective rim thickness`
- `feat(retopo): add constrained modular seam reduction`
- `feat(retopo): add screen-space global slot allocator`
- `fix(hair): break regular crown occlusion boundaries`
- `qa(identity): calibrate top-down class recognition contract`

Final file:
`AETHERQOR_V6_FINAL_TECHNICAL_DECISIONS.md`

Table:
| Problem | Before | Tested | After | Visual PASS | Runtime PASS | Decision | Status | Commit |

Allowed final statuses only:
- IMPLEMENTED_PASS
- IMPLEMENTED_FAIL with exact reason
- RESEARCH_CONFIRMED_NEEDS_ENGINEERING
- BLOCKED_EXTERNAL

Forbidden: RESEARCH_DONE.

Start with P1/P2. Do not stop after presenting a plan; continue into real experiments automatically.
